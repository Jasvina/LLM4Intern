# Post Training - 真实的业务场景中 SFT 之后为什么还需要 RL
想清楚再动手,本文以垂直域 Tool Call Agent 为例

## 背景
做完 SFT 之后,模型已经学会了基本的工具调用格式、意图路由和标准流程。这时候引入 RL / GRPO,目标不应该是「再教模型一遍怎么调工具」——SFT 已经完成了这件事。

RL 在这个阶段真正该补的是:当存在多条合法轨迹时,如何更稳定地选到任务得分更高的那条。

这是 SFT 和 RL 的本质分工差异,也是很多工程实践中容易混淆的地方。

## SFT 和 RL 的分工边界
SFT 擅长把有明确 supervision 的能力学进去:
- 这类 query 大概该调哪个工具
- 工具调用的格式长什么样
- 一条标准流程从头到尾怎么写

但 Agent 任务里最难的不是记答案,而是在 rollout 过程中做选择。同一个任务往往不止一条合法 trace,训练目标应该从「模仿一条 gold 轨迹」转变为「在可能的轨迹中拿更高的任务分」。

这个转变,正是 RL 的价值所在。

## RL 值得强化的五类能力
### 1. 补「选择」而不是补「知识」
SFT 对每个能力点的优化信号是局部且均匀的——它不区分「差一点」和「差很多」,也不会为整条轨迹的最终结果负责。

RL 最有价值的地方,是让模型为以下决策负责:
- 少走弯路,减少不必要的工具调用
- 选错工具时能收到明确的惩罚信号
- 参数填错的代价被直接反映在 reward 上
- 该停的时候停,而不是继续空转
- 该追问的时候追问,而不是瞎猜参数
- 该拒绝的时候拒绝,而不是偶尔漏掉

这些能力不是 SFT 没有覆盖,而是 SFT 对它们的优化信号太弱、太均匀,无法驱动模型在策略层面真正负责。

### 2. 强化多步决策质量
纯单工具问答之外,Agent 任务更复杂的地方在于多步决策:

| 能力 | SFT 能做到什么 | RL 能补什么 |
|------|----------------|-------------|
| 顺序调用 | 见过例子,能模仿 | 真正按依赖顺序执行,不乱序 |
| 并行调用 | 知道格式 | 稳定压到同一个 assistant turn |
| 追问澄清 | 知道该追问 | 信息不足时真正执行,不瞎调 |
| 拒答 | 见过拒答样本 | 面对越权请求稳定拒绝,不漏 |

SFT 可以让模型「见过例子」,但策略层面的稳定性——在压力下是否真的执行出正确行为——是 RL 的核心优化目标。

### 3. 让 benchmark 指标直接成为训练信号
一个值得关注的工程选择:让 reward 直接来自评测框架的规则化打分,而不是另训一个 reward model。

这意味着 RL 真正在做的事是:
- 把评测最关心的能力直接变成训练信号
- 让模型为工具选择准确率、参数字段准确率、顺序/并行执行、追问/拒答行为、端到端成功率这些指标直接负责

RL 的价值不是「让模型更懂业务领域」,而是「让模型更会为任务成功率负责」。这是 RL 比 SFT 更直接的地方。

### 4. 小模型最现实的收益:稳定性和边界行为
对于 1-2B 量级的小模型,不太可能靠 RL 获得很强的开放式推理飞跃。但以下方向的改进是现实的:

| 改进前 | 改进后 |
|--------|--------|
| 经常差一点 | 更稳定 |
| 会做但偶尔乱来 | 少犯低级错误 |
| 单步不错 | 多步更稳 |
| 知道该拒绝/追问 | 真正稳定执行出来 |

这类改进在小模型 Agent 场景里高度实际,不需要参数量的飞跃,只需要更精准的 reward 信号。

## RL 不擅长解决的问题
同样重要的是,明确哪些地方不该指望 RL 解决:
- **domain knowledge 缺口**:如果训练数据本身没有覆盖某类业务场景,RL 无法凭空补足这个知识——这是数据覆盖问题,不是 policy 优化问题。
- **工具 schema 不清或 gold label 不准**:Reward 错了,RL 只会放大错误。在 reward 设计可信之前,引入 RL 会加速模型往错误方向收敛。
- **长链抽象推理能力**:RL 更适合做 policy refinement,不是能力跃迁。指望小模型通过 GRPO 产生类似大模型的长链推理涌现,在工程上不现实。

## SFT 和 RL 的能力分配原则
一个实用的判断框架:

| 问题类型 | 更适合 SFT 解决 | 更适合 RL 解决 |
|----------|-----------------|----------------|
| 训练数据里没有覆盖这类场景 | ✓ | |
| 格式/结构学不对 | ✓ | |
| 多条合法轨迹,需要选更优的 | | ✓ |
| 单步对但多步不稳 | | ✓ |
| 边界行为(追问/拒答)执行不稳定 | | ✓ |
| reward 信号可以被规则化定义 | | ✓ |

**核心判断标准**:如果问题的根因是「模型没见过这类数据」,扩充训练集更有效。如果问题的根因是「模型见过但执行不稳定、策略不优」,RL 更有效。

## 一句话总结
对垂直域小模型 Agent 来说,RL / GRPO 最合理的目标是:

把一个「已经会做工具调用的 SFT agent」,进一步优化成一个在多步工具任务中更稳、更少犯错、更加 reward-aligned 的小模型 agent。

RL 不是 SFT 的替代,而是 SFT 之后的 policy 精炼阶段。两者解决的是不同层次的问题,混用目标会让两个阶段都达不到预期效果。

小红书号:942470716
